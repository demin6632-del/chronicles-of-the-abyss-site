const {sign}=require('../lib/token');
async function getPayment(id){const shop=process.env.YOOKASSA_SHOP_ID,key=process.env.YOOKASSA_SECRET_KEY;if(!shop||!key)throw new Error('ЮKassa API не настроен');const r=await fetch('https://api.yookassa.ru/v3/payments/'+encodeURIComponent(id),{headers:{Authorization:'Basic '+Buffer.from(shop+':'+key).toString('base64')}});const data=await r.json();if(!r.ok)throw new Error(data.description||data.message||'Не удалось проверить платёж');return data}
module.exports=async(req,res)=>{try{const id=req.query&&req.query.payment_id;if(!id){res.statusCode=400;return res.end(JSON.stringify({error:'payment_id required'}))}const p=await getPayment(id);if(p.status!=='succeeded'||p.paid!==true||p.amount?.value!=='300.00'||p.amount?.currency!=='RUB'||p.metadata?.dlc_id!=='abyss-awakening'){res.statusCode=409;res.setHeader('Content-Type','application/json; charset=utf-8');return res.end(JSON.stringify({paid:false,status:p.status}))}const code=sign({v:1,dlc_id:'abyss-awakening',payment_id:p.id,amount:'300.00',currency:'RUB'});res.setHeader('Content-Type','application/json; charset=utf-8');res.end(JSON.stringify({paid:true,code}))}catch(e){res.statusCode=500;res.setHeader('Content-Type','application/json; charset=utf-8');res.end(JSON.stringify({error:e.message}))}};
